// ignore: unused_import
import 'package:flutter/material.dart';

class OhmCalculatorPage extends StatelessWidget {
  const OhmCalculatorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Kalkulator Hukum Ohm")),
      body: const Center(
        child: Text("Fitur Kalkulator Hukum Ohm akan segera hadir!"),
      ),
    );
  }
}


